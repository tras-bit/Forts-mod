-- ============================================================
--  ЗРК / ПРО: звук пуска (залп)
-- ============================================================
--  БЫЛО: Volume = 1.0 при сэмпле, нормализованном почти в 0 dBFS
--  (RMS -0.1 dBFS — ровная стена звука на всю шкалу).
--  ЗРК стреляет залпами (RoundsEachBurst 2-3), поэтому за секунду
--  накладывалось 2-6 таких клиппующих сэмплов подряд.
--
--  СТАЛО: 0.30 + затухание по расстоянию + приоритет ниже игровых.
-- ============================================================
LifeSpan = 2.0

Effects =
{
    {
        Type = "sound",
        Sound = path .. "/audio/sam_single_shot.ogg",
        TimeToTrigger = 0,
        PlayForEnemy = true,
        Volume = 0.30,          -- было 1.0
        Priority = 112,
        Falloff = true,
        FalloffStart = 1500,
        FalloffEnd = 6000,
    },
}
