-- ============================================================
--  ГСН-ракета: звук захвата/пуска (lock_and_launch)
-- ============================================================
--  БЫЛО: Volume = 1.0 при том, что сам .wav нормализован по пику
--  (RMS ~ -0.7 dBFS, пик 0 dB) — то есть игра играла его буквально
--  на пороге клиппинга. Пуск ПЗРК — частое событие, поэтому это
--  и была вторая по вкладу «пизда ушам» (после писка наведения).
--
--  СТАЛО: 0.30 + затухание по расстоянию + низкий приоритет.
--  FalloffStart/FalloffEnd — реальные поля движка, ваниль ставит
--  их, например, в effects/barrel_explode.lua (1000..3000).
-- ============================================================
LifeSpan = 2.0

Effects =
{
    {
        Type = "sound",
        Sound = path .. "/audio/lock_and_launch.ogg",
        TimeToTrigger = 0,
        PlayForEnemy = true,
        Volume = 0.30,          -- было 1.0 при клиппующем сэмпле
        Priority = 96,          -- ниже обычного: пусть вытесняется
        Falloff = true,
        FalloffStart = 1200,
        FalloffEnd = 5000,
    },
}
